CREATE TABLE sales_data (
    OrderID INT,
    Customer VARCHAR(50),
    Category VARCHAR(50),
    SalesAmount DECIMAL(10,2),
    Country VARCHAR(50),
    OrderDate DATE
);

SELECT * FROM sales_data;
